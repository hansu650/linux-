# 17周编译内核 增加一个系统调用
## 中断
- cpu等外设太慢了
- 现在cpu和外设并行，工作完了要告诉cpu 
- 通知机制-中断-让cpu和外设能并行工作
- 程序运行之间 在内核态和用户态切换
- 内核态才能中断 保护现场 返回现场 处理完再返回用户态
## 系统调用
- 用户程序要使用外设
- 向操作系统提出了一个申请
### 共同点都是保护现场，进入操作系统 还原现场  处理流程完全一样  都是操作系统给你提供功能
### 区别
1. 中断 异步 我不知道外设啥时候用完
2. 系统调用 同步

- 写操作系统的代码都是从中断开始写
- 按键盘有两次中断
# 增加一个系统调用
- 65面
- 位于用户空间进程和硬件设备之间的一个中间层
- 为用户态进程与硬件设备交互提供一组接口
## 保存的现场在系统堆栈里面，内核的空间里
- 知识点都是有联系的

- 看一下系统调用有多少个400-600

## 两种库函数
- sin cos 不用操作系统 只需要在用户态写就行
- 是系统调用(调用系统的软硬件资源) scanf printf fopen  fork  把系统调用封装了api 

# 所以我们自己完成系统调用也要进行封装

- 汇编只有号没有名字
- c语言通过名字printf找到系统调用号
- 号*4+总表首地址  =入口地址       一个号4个字节
- 总控程序恢复现场切换到用户态

# 改三个文件
- 服务例程  系统总调用表的最后 系统调用号

## 保存两次
- 第一次保存必需cs:ip/pc 而且切换态的话这个可能被覆盖掉 第二次保存剩下
# 81 82 83 84 91 95
# 内核函数 我们只能用这个因为已经在内核了   

# 改完三个 编译好内核之后还要写个测试代码 
- 先封装后调用 打印出来的结果看就行


# 改内核很麻烦 插模块很简单 
- 我们装设备驱动很简单啊 其实是插了一个模块


# 模块是为了写设备驱动
- lsmod 看所有的模块
- su 超级用户
- 一样在日志文件里面打印
- 所以不要插入模块
#  **ls -l/dev**这个讲过很多遍 
- b开头的block 存储设备 块设备 
- 都是字符设备，块设备比较复杂
- 主设备号+次设备号
# linux 一切皆文件
- vfs 统一管理层 win没有所以只能用nfsw
- 设备是一个文件 所以Open read write 

- 动态分配设备号简历字符设备的数据结构
- cdev_add
- 根据板子硬件说明书来写






