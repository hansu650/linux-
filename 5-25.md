### fork
- 子进程由父进程fork 的话就同步继续一样执行,因为pc一样
- 父进程的缓冲区要是没有\n 或者flesh这种清掉那么缓冲区也会copy的
- 子进程可以return 不影响父进程
- 打印到缓冲区里面



### vfork
- 共享所有资源 所以子进程return 父进程也相当于return 就把所有资源都扔掉了 会出错


# 多线程
- 系统监视器
- 多个核心，每个核一条曲线
- 心形两个核 直线50% sin 各一个
- 颜色稍微亮一些 调整一下
- 保证其他的核在底下 所以尽量不要开别的进程免得干扰使波动太大
- 把代码看懂
- 还可以多加一点形状比如三角形

## 进程是资源的基本单位 线程是处理器调度的基本单位
- ps -aux 
- s r i l 各种进程的状态
- l 多线程的首线程
- ps -T -p 2732
- pid进程 spid线程号
- 线程的数据结构还是和进程一样

## 内核线程 []   父进程是2号进程
- 没办法操控，操作系统创建的
- 共享资源 

## 用户线程 2种
1. clone 轻量级进程
- int clone(int (*fn)(void * arg),void *stack, int flags, void *arg);

2. 线程库提供的
- int pthread_create(pthread_t *restrict tidp, const pthread_attr_t *restrict attr, void *(*start_rtn)(void), void *restrict arg);
# 知道为什么  不管问不问ai
- -lpthread 
- 非标准库l link一下
- 进程之间要互相控制 同步

- 比如买机票 同时访问数据库
- read 随便
- write 排他
- 多进程 上锁write的时候
- 互斥(一种)

- 

- int pthread _create(pthread_t *thread, const pthread_attr_t*attr,void *(*start_rtn) (void*), void *arg);
-                     线程库分配的序号       
- *args 参数传递很容易错，如果只有一个变量但是只有一个地址那就以最后一次修改为准
- 所以采才用数组

- 主线程和子线程id不一样
- id很多的

- 代码都在09source code里面

## 同步异步

- 同步-退出exit  join 
- 异步-分离
- 不然内存可能泄露

## cpu利用率
- 编程之美 刷题的 解决实际问题 不像传统算法题

- 打印50%的线  用cpu的频率算时间 for空循环 循环次数 汇编数个数
- 改进  因为cpu频率不同 所以gettime直接拿time<10ms 就行了

## 所以核心就是运行多长时间，休眠多长时间
- 正弦 
- 取样200个点 200/2派=0.01派 间隔
- 还要上移sin 
- busy 就是正的 idle停的就是100%-busy

- heart
-  usleep 微秒
- sleep 毫秒
















