# 计算机网络的梗概
- 理论比较多，没必要很多实验
- 分层

- 进程到进程的通信
- apache服务器是主机上面的一个进程

##  osi 国际标准 
- ISO 国际标准组织


1. 应用层http  公司的老板
- get index.html get a.jpg
2. 表示层
- 乱码 编码不一样  unicode  浏览器utf-8
- tls 表示层协议，加密数据解密数据

3. 会话层
- ssl 会话层协议  建立一个加密信道
- 提供同步点 10% 20% ... 保证20%一定在客户端存在，要是中断会回滚到前一个同步点接着下载
- 断了之后可以接着下载音频，视频文件

4. 传输层
- 每个员工都通信的话 链接建立的很多开销大，传达室
- 发送端接受这个公司的所有信件发到对面的传达室再去给别人
- 根据**端口号**来进行数据的分发
- 进程到进程的通信(通过5ip地址来进行定位网络层送过去，到目标传输层再去分发)


5. 网络层
- 传输层给你**ip地址**层 邮局
- 从源地址送到目的地址，主机到主机的通信

6. 数据链路层
- 会经过很多相邻的节点
- 一步一步到达目的地址
- 完成相邻节点的通信
- 通过 硬件地址传递的
- ifconfig
- **micro 地址** 网卡地址
- 快递公司的中间站点



---
7. 物理层
- 传输介质 双脚线，光纤
- 搬运工人

## 先从上往下传过去，再通过信号从下往上转换


##  tcp/ip  事实标准，企业提出来的
- 先发制人 现在相互渗入了

# tcp 传输层 
# ip  网络层

- 最下面两层是空的，因为不关心这个 通信网络定义的 由18几几年的专家去研究
- 计算机网络只在上面研究，下面都支持
- 上面三层是引用层

1. 不是通用的就不写到通用的了
2. 因为现在都是加密解密了，所以tls变成表示层加进去
3. 以前http都是明文传输所以不用加密解密
4. 不是所有的都需要同步点
5. 因为http可以传视频就有 传网页就没有同步点
6. openssl在库里面装
7. 定义了接口 自己系统调用
# tcp
- 面向连接的可靠传输 保证收到和数据一定是对的
- http 1.0 2.0


# udp
- 视频丢一两帧
- http 3.0

# linux支持所有的文件系统
- 映射到同一个套接字就行
- ip 端口号 协议(tcp/ip) 网络进程三要素
- 链接的话5要素 ipx2 端口号x2 协议(tcp/ip)


# peer to peer
- 保证两端的代码是一样的
- 高性能并发

- ping 
- socket
- x86 小端 低字节低地址，高字节高地址
- 网络字节是大端

- 所以进行一下转化
