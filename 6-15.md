# 6-15
# ip 端口号 协议(tcp/ip)
- tWPD
功能
sockaddr的另一种表示形式
struct in_addr{
结构
_u32 s_addr;
struct sockaddr_in{
};
short int sin_family;
/*地址族*/
提取文字
更多
unsigned short int sin_port; /* 端口号 */
struct in_addr sin_addr; /* IP地址*/
unsigned char sin_zero[8];/*填充0以保持与struct sockaddr同样大
小*/
;
说明
sin_zero用于将sockaddr_in结构填充到与struct sockaddr等长，可用
bzero()或memset()函数将其置为0
当sin_port=0时，系统随机选择一个未被使用的端口号
当s_addr=INADDR_ANY时，表示填入本机IP地址
指向sockaddr_in的指针和指向sockaddr的指针可以相互转换

- 现在af pf没区分了
- htonl host to network lonog 主机和网络五项转换因为大端小端顺序不一样


# socket 
- 创建一个socket描述一个网络通信的进程
- 第一步
1. 函数原型
-  int socket(int domain, int type, int protocol);
2. 参数说明
- domain：应用程序所在主机使用的通信协议族，即地址族
- type:套接字类型，可选流式、数据报式或原始式套接字
- protocol:使用的特定协议，通常设置为0，让内核根据指定的类型
和协议族使用默认的协议

3. SO_RCVBUF    发送缓冲区大小    SO_SNDBUF  接收缓冲区大小

4. ip级别的选项 计网的内容
- time to live ttl 到时间就消失了
- 写ping用到这个
- 可以用这个时间判断linux和win

5.  bind socket赋值
6. connect 本地socket去通信外部的
7. 80 443 端口号 
8. dos攻击，我们一直用tcp链接 每个服务器都有一个最大的backlog
9. accept listen到了一个链接443 然后服务器监听到了我们的链接用accept生成另一个socket  新的端口号来传输数据
- linux一切皆文件，设备，网络编程都是文件，返回的是一个标志socket

10. 我们发送数据接收数据读写内容都是文件
11. send 因为之前已经建立了通道所以不用地址，直接tcp/ip 通过accept的新端口号发过去
12. recv 本地接受
13. 默认框架都是阻塞框架c语言 如果有数据发送成功继续执行，没有数据发送不会往下执行停在这里
- 和wait一样，等子程序发信号才执行 
14. 现在一般直接在socket里面定义阻塞非阻塞

15. udp sendto 要告诉进程我要发给谁  recvfrom  要有对方的ip 端口号

16. close 归还资源 不关listen的端口，关accept生成的数据端口

17. 真正的ip：(C0A80001)是192.168.0.1 后面是字符串，前面的才是()真正的32位ip
# 基于tcp c/s模式 
18.  一个clinet 一个server 没有并发 
19. pserver文件 主进程只监听，fork子进程完成数据的收发  多进程的
20. z 僵尸进程，子进程的资源没有回收 
- 但是我们不能等子进程退出才能进行，我们要一直listen
- 所以我们用 waitpid  

21. 可以试试多线程开销更小 
## 改进方法
1. 改成多线程 直接就没有僵尸进程了
2. 加上waitpid僵尸进程浪费资源


## 基于udp 实现peer to peer
- 先来cs模式
1. 参数都传进去了
2. dglib要一起编译，是一个函数定义问及那

3. ./server 8000  ./client 127.0.1.1 8000

4. 高性能 监听到第一个数据发过来就新建一个进程，也是一个client新建一个进程为他服务 pserver

### 外面的都是peer to peer
1. udp是有数据就发，所以for(;;) 无限循环
- ./udptalk 127.0.1.1 6000 127.0.0.1 5000
- ./udptalk 127.0.0.1 5000 127.0.1.1 6000
- 这个是对的
# 也可以改进，还是串行的
1. 改成多线程
2. 保留单线程-只监听一个端口，处理完再去处理下面的     
- io多路复用的模型 select 同时监控多个端口，一个个扫描,   同时监控多个socket    还是一个一个看 同步的多路io
- 异步的多路io epo函数的原理 做好了他自己会通知你的，不是真正的异步io(数据准备好了还要你自己做)

- 真正的异步io aio 他帮你全部做完了 (数据做完了接收完了)  最近几年才出现的
# 通过一个技术的历史找到真正的创新点，解决了原来技术的什么问题
- 涉及到io模型的演变 5种，


2. udptalk22 有问题的
- 没收到  因为阻塞fgets导致只有接收端敲回车才行
- sockets改成非阻塞才行 还有标准输入
- fcntl(sockfd,F_SETFL,O_NONBLOCK);



- 下节课讲两段代码，原始套接字

- windows四个返回值
- linux返回一个包  ping的ip是dns解析  第一个是几个数 ttl time to live  
- linux服务器 从64开始
- 后面还有往返时间