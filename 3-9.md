# 第13页：GNU 与 GPL

## 这一页在讲什么

这一页其实是在讲 **Linux / 开源软件世界最核心的思想基础**：

**GNU 是一个自由软件项目，GPL 是保障“自由软件自由性”的许可证。**

也就是说：

* **GNU** 解决的是“我要做什么”
* **GPL** 解决的是“做出来的软件按什么规则给别人用”

---

# 一、GNU 是什么

## 1. GNU 的本质

GNU 是一个 **自由软件工程项目**。

老师这页写的是：

> GNU is a free software engineering project

这句话的意思不是“GNU 是免费的软件项目”，而是：

**GNU 是一个以“软件自由”为核心目标的软件工程计划。**

这里的 **free** 更准确理解成：

* 不是“免费”
* 而是“自由”

也就是用户应该拥有使用、研究、修改、传播软件的自由。

---

## 2. GNU 想做什么

GNU 项目的目标，是想构建一个 **类 Unix 的、完全自由的软件系统**。

你可以把它理解成：

> GNU 想搭建一整套自由的软件世界，包括编译器、编辑器、命令行工具、库等等。

后来 Linux 内核出现后，很多 GNU 工具和 Linux 内核组合起来，才形成了我们现在常说的 **GNU/Linux** 系统。

所以严格一点说：

* **Linux**：更偏向“内核”
* **GNU**：提供了大量系统工具和自由软件理念支持

---

## 3. GNU 为什么重要

因为它不仅仅是写软件，更重要的是提出了一种思想：

**软件不应该只是给你“用”，还应该让你有权利去学习、修改和分享。**

这就是后面 GPL 出现的背景。

---

# 二、GPL 是什么

## 1. GPL 的定义

PPT 里写的是：

> The GPL (General Public License) ...

GPL 全称是：

**GNU General Public License**
中文一般叫：

**GNU 通用公共许可证**

它是一种软件许可证。

---

## 2. GPL 是干什么的

它的作用是：

**用法律条款把“软件自由”保护下来。**

也就是说，开发者不是随便口头说“你可以自由用”，而是通过 GPL 这种许可证，正式告诉别人：

* 你可以怎么用
* 你可以怎么改
* 你可以怎么传播
* 你传播时要遵守什么规则

---

## 3. PPT 里这句怎么理解

原文大意是：

> GPL 是一种版权形式，是 Richard Stallman 在分发开源软件的实践中逐渐形成的一套保护自由软件的条款。

更容易懂的说法是：

**GPL 不是取消版权，而是利用版权法来保护自由。**

这个点很重要。

很多人会误会：

* 开源 = 没有版权
  其实不是。
* 开源 = 想怎么拿就怎么拿
  也不是。

正确理解是：

**GPL 依然建立在版权之上，但它要求后续使用者不能把这种自由拿走。**

---

# 三、Richard Stallman 是谁

这页提到了 **Richard Stallman**，简称 **RMS**。

他是自由软件运动的重要代表人物，也是 GNU 项目的发起者之一。

你可以记成一句话：

**Stallman 提出了自由软件思想，并推动了 GNU 和 GPL 的形成。**

考试或者老师提问时，记住这层关系就够了：

* GNU：项目
* Stallman：核心推动者
* GPL：为 GNU / 自由软件提供法律保障的许可证

---

# 四、GPL 赋予用户哪些“自由”

PPT 上列了三条核心内容，我们一个一个讲。

---

## 1. Freedom to run this program for any purpose

中文：

**可以为了任何目的运行这个程序。**

### 这句话什么意思

就是你拿到 GPL 软件后，可以自由使用它：

* 学习用
* 商业用
* 个人用
* 学校实验用
* 公司服务器上用

一般不能有人限制你说：

* “只能教学使用”
* “不能商用”
* “只能在某台电脑上运行”

GPL 强调的是：

**你有运行软件的自由。**

### 记忆点

这条自由关注的是：

**“能不能用”**

---

## 2. Freedom to redistribute copies

中文：

**可以重新分发副本。**

### 这句话什么意思

如果你拿到一个 GPL 软件，你可以把它再发给别人：

* 发给同学
* 发给朋友
* 放到镜像站
* 给实验室成员用

这就是“复制和传播”的自由。

### 为什么重要

传统闭源软件通常会限制复制和传播，比如：

* 一套许可证只能一台电脑
* 禁止私下转发安装包
* 禁止二次分发

而 GPL 的思想是：

**软件自由不应该只停留在你自己手里，还应该能传递给别人。**

### 记忆点

这条自由关注的是：

**“能不能传”**

---

## 3. Freedom to improve the program and release the improvements publicly

中文：

**可以改进这个程序，并且公开发布你的改进版本。**

PPT 后面还补了一句：

> provided the source code is available

也就是：

**前提是源代码可获得。**

### 这句话什么意思

你不仅能用，还能改。

比如：

* 修改 bug
* 增加新功能
* 优化界面
* 提高性能
* 按自己的需求定制

而且你修改完以后，还可以把你改过的版本发布出去，让更多人受益。

### 为什么这条最关键

因为这体现了自由软件最核心的一点：

**用户不是被动使用者，而是可以参与改进的软件共同建设者。**

闭源软件里，你通常看不到源代码，就谈不上修改。
而 GPL 软件鼓励的是：

**让软件不断被改进、共享、再改进。**

### 记忆点

这条自由关注的是：

**“能不能改、能不能公开分享改进版”**

---

# 五、这里其实还隐含了一个重要前提：源代码

PPT 最后一条里已经提到 source code。

这说明一个核心逻辑：

**没有源代码，就谈不上真正的修改自由。**

因为：

* 看不到代码，没法研究
* 不能研究，就没法改
* 不能改，就无法真正继承自由

所以开源许可证特别强调 **source code availability（源代码可获得）**。

---

# 六、这页 PPT 里没明说，但你最好补充知道的一点：GPL 的“传染性 / 继承性”

这个在考试、答辩、老师追问时很可能有用。

GPL 最有代表性的特点就是：

**如果你分发了基于 GPL 软件修改出来的版本，通常也要继续遵守 GPL。**

通俗说：

> 你享受了前人的自由，就不能在你这里把自由截断。

这也叫 **copyleft（著佐权 / 反版权式共享）** 思想。

你可以把它和普通开源理解成区别：

* 有些开源许可证比较宽松，你改完可以变成闭源
* **GPL 更强调“自由必须继续传下去”**

这个是 GPL 最有辨识度的地方。

---

# 七、LGPL 是什么

PPT 最后突然出现了一个 **LGPL**，老师应该是在顺带提。

LGPL 一般是：

**Lesser General Public License**
中文常译：

**较宽松的 GPL 许可证**

## 它和 GPL 的关系

你可以简单记：

* **GPL**：要求更严格
* **LGPL**：比 GPL 更宽松，常用于库（library）

## 为什么会有 LGPL

因为有些库如果强制要求太严格，会影响它们被广泛使用。
所以 LGPL 允许在一定条件下，被其他软件调用时更灵活一些。

## 最简单记法

考试里记一句就够了：

**LGPL 是 GPL 的较宽松版本，常用于软件库。**

---

# 八、这一页的核心逻辑串起来是什么

你可以把整页压缩成这样一条线：

**GNU 提出自由软件目标 → Stallman 推动自由软件运动 → GPL 用法律方式保护这种自由 → 用户因此拥有使用、复制、修改、发布改进版的权利。**

---

# 九、老师上课可能会问的点

## 1. GNU 和 GPL 的区别

这个最容易考。

标准回答：

* **GNU 是一个自由软件项目**
* **GPL 是 GNU 推广使用的一种许可证**

一句话记忆：

**GNU 是“项目/运动”，GPL 是“规则/协议”。**

---

## 2. GPL 保护的是什么

不是单纯保护作者赚钱，
而是：

**保护软件使用者的自由，以及这种自由在传播过程中不被剥夺。**

---

## 3. free software 的 free 是什么意思

不是免费，
而是：

**自由**

这点非常重要。

---

## 4. 为什么一定要有源代码

因为没有源代码，就不能：

* 学习程序怎么写
* 修改程序
* 再发布改进版

所以源代码是软件自由的基础条件之一。

---

# 第 14 页：Ubuntu installation

这一页原文核心是：

* **Partition：**

  * `swap`
  * `/`
  * `(/home /boot)`
* **Linux file system type: ext4**
* **windows: NTFS**

这页其实就是在讲两个问题：

1. **Ubuntu 安装时磁盘怎么分区**
2. **Linux 和 Windows 常用的文件系统有什么不同**

---

# 一、这一页整体在讲什么

你可以先把这页的中心思想记住：

> 安装 Ubuntu 时，至少要给 Linux 准备合适的分区；最基本的是根分区 `/`，常见还会配 `swap`，有时会单独分 `/home` 和 `/boot`；Linux 常用文件系统是 `ext4`，而 Windows 常用的是 `NTFS`。

---

# 二、什么叫分区（Partition）

## 1. 分区的本质

分区就是把一块硬盘，逻辑上划分成若干块区域，每块区域承担不同作用。

比如一块硬盘可以分成：

* 一块给 Windows
* 一块给 Ubuntu
* 一块专门放用户文件
* 一块作为交换空间

所以“安装 Ubuntu”并不是简单把系统复制进去，而是要先决定：

**磁盘空间怎么分给 Linux 用。**

---

## 2. 为什么 Linux 要讲分区

因为 Linux 的文件系统组织方式和 Windows 不一样。

Windows 常见是：

* C 盘
* D 盘
* E 盘

Linux 不是按“盘符”来组织，而是按“目录树”来组织。
所有内容最终都挂在一个统一的目录结构下面。

所以 Linux 安装时会问你：

* 哪一块分区用作根目录 `/`
* 哪一块分区用作交换分区 `swap`
* 要不要单独拿一块给 `/home`
* 要不要单独拿一块给 `/boot`

---

# 三、`/` 是什么

这一页里最重要的就是 `/`。

## 1. `/` 叫根目录

Linux 中的 `/` 叫 **根目录**，是整个文件系统的起点。

你可以理解成：

**Linux 的所有文件和目录，最终都从 `/` 往下展开。**

例如：

* `/home`
* `/boot`
* `/etc`
* `/usr`
* `/var`

这些全都属于根目录下面的内容。

---

## 2. 为什么 `/` 分区最重要

因为没有 `/`，系统就没地方装。

Ubuntu 的系统文件、配置文件、命令程序、库文件等，最终都需要放在根分区里。

所以安装 Linux 时：

**根分区 `/` 是必须有的。**

---

## 3. 根分区里一般装什么

常见包括：

* 系统核心文件
* 命令程序
* 配置文件
* 软件运行依赖
* 一部分用户环境文件

所以如果只做最简单安装，通常只要：

* 一个 `/`
* 再加一个 `swap`

就能装起来。

---

# 四、`swap` 是什么

这一页第二个关键词是 `swap`。

## 1. swap 的中文理解

`swap` 一般叫：

* 交换分区
* 交换空间

它的作用类似于：

**把硬盘的一部分临时当成内存的补充。**

---

## 2. 为什么需要 swap

当物理内存 RAM 不够时，系统可以把暂时不用的数据移到 swap 中，腾出内存给当前任务使用。

所以它本质上是：

**虚拟内存机制的一部分。**

---

## 3. swap 的作用

主要有两个：

### （1）补充内存

如果开了很多程序，内存吃紧，系统可能会使用 swap。

### （2）休眠支持

有些情况下，系统休眠会需要足够的 swap 空间来保存内存内容。

---

## 4. swap 是不是越大越好

不是。

因为 swap 毕竟在硬盘上，速度通常比真正内存慢得多。
所以它只能“救急”，不能替代内存。

你可以记：

* **RAM 快**
* **swap 慢**
* swap 是补充，不是替代

---

## 5. 现在还一定要单独建 swap 分区吗

不一定。

现代 Ubuntu 有时会使用 **swap file（交换文件）**，而不是单独的 swap 分区。
但从教学和考试角度，老师这里讲 `swap`，你就按“交换空间”理解即可。

---

# 五、`/home` 是什么

这一页里写了：

`(/home /boot)`

这说明老师想表达的是：

**`/home` 和 `/boot` 可以单独分出来，也可以不单独分。**

---

## 1. `/home` 的作用

`/home` 主要存放普通用户的数据和个人配置。

比如：

* 用户文档
* 下载文件
* 桌面文件
* 用户个性化设置
* 部分软件的用户级配置

例如你的用户目录可能是：

* `/home/你的用户名`

---

## 2. 为什么有人喜欢单独分 `/home`

因为这样做有一个很大好处：

**系统和用户数据可以分开。**

比如以后你重装 Ubuntu：

* 系统分区 `/` 可以重新装
* 用户数据分区 `/home` 可以尽量保留

这样文档和个人设置更容易保住。

---

## 3. `/home` 是必须的吗

不是必须。

如果你不单独分 `/home`，它也会作为根分区 `/` 下面的一个普通目录存在。

所以：

* **必须的是 `/`**
* **`/home` 可以单独分，也可以不单独分**

---

# 六、`/boot` 是什么

## 1. `/boot` 的作用

`/boot` 用来存放启动相关文件。

例如：

* 内核文件
* initramfs
* 启动加载器相关文件

你可以理解成：

**电脑启动 Linux 时，要先用到这里面的关键文件。**

---

## 2. 为什么有时要单独分 `/boot`

在某些特殊情况下，单独分 `/boot` 会更方便，比如：

* 老式机器兼容性问题
* 特殊磁盘布局
* 多系统环境
* 某些加密场景

但对于普通 Ubuntu 安装，很多时候不一定非要单独分 `/boot`。

---

## 3. `/boot` 和 EFI 的区别

这个老师这页没展开，但你最好顺手记一下：

* `/boot` 是 Linux 启动文件目录
* **EFI System Partition** 是 UEFI 固件启动时常用的引导分区，通常格式是 FAT32

现代电脑里，经常还会碰到 EFI 分区。
这个和 `/boot` 不是完全一回事。

考试如果老师只按这页讲，你记 `/boot` 是“启动相关分区/目录”就行。

---

# 七、这一页想表达的分区方案是什么

## 1. 最简单方案

最简单可以是：

* `/`
* `swap`

这种方式结构简单，适合入门安装。

---

## 2. 稍微规范一点的方案

可以是：

* `/`
* `swap`
* `/home`

这样系统和用户数据分开，后续维护更方便。

---

## 3. 再细一点的方案

可以是：

* `/`
* `swap`
* `/home`
* `/boot`

这种更细化，但也更复杂。

---

## 4. 所以这一页括号里的意思

PPT 写成：

`swap`
`/`
`(/home /boot)`

就是在告诉你：

* `swap` 常见
* `/` 必须有
* `/home` 和 `/boot` 可以视情况单独分出来

---

# 八、ext4 是什么

这一页下半部分写：

> Linux file system type: ext4
> windows NTFS

这部分是在讲 **文件系统格式**。

---

## 1. ext4 是 Linux 常见文件系统

`ext4` 是 Linux 下非常常见的文件系统类型。

安装 Ubuntu 时，普通分区一般会格式化成 ext4。

你可以把它理解成：

**Linux 世界里最常用、最经典的一种文件系统。**

---

## 2. ext4 的特点

课堂里记住这些就够了：

* 稳定
* 常用
* 适合 Linux 系统安装
* 支持较大的文件和分区
* 有日志功能，可靠性较好

---

## 3. 为什么 Ubuntu 常用 ext4

因为它兼顾了：

* 稳定性
* 性能
* 兼容性
* 成熟度

所以初学者安装 Ubuntu 时，一般默认就选 ext4。

---

# 九、NTFS 是什么

## 1. NTFS 是 Windows 常见文件系统

Windows 系统一般常见的是：

**NTFS**

特别是 Windows 的系统盘、数据盘，大多数都是 NTFS。

---

## 2. NTFS 和 ext4 的区别

最核心记忆就是：

* **Linux 常用 ext4**
* **Windows 常用 NTFS**

它们是不同操作系统生态里常见的文件系统格式。

---

## 3. 为什么安装 Ubuntu 时要提 NTFS

因为很多同学装 Ubuntu 时，电脑上原本已经有 Windows。
而 Windows 分区通常是 NTFS。

这时候就要知道：

* Windows 原来的分区大概率是 NTFS
* Ubuntu 自己安装用的 Linux 分区通常要设成 ext4

也就是说：

**不要把 Ubuntu 的系统分区装成 NTFS。**

---

# 十、这页在安装实操中的实际意义

你可以把这页理解成装系统前必须知道的“最低分区常识”。

安装 Ubuntu 时，你至少要明白：

## 1. 哪个分区最关键

就是 `/`

没有它，Ubuntu 没法正常安装。

## 2. swap 是干什么的

是交换空间，辅助内存使用。

## 3. `/home` 和 `/boot` 要不要单独分

可以分，也可以不分，看需求。

## 4. Linux 分区一般格式化成什么

一般是 **ext4**

## 5. Windows 原分区一般是什么

一般是 **NTFS**

---

# 十一、老师可能会问的几个点

## 1. Linux 安装时必须要有哪个分区

答：

**根分区 `/` 是必须的。**

---

## 2. swap 的作用是什么

答：

**swap 是交换空间，用于在内存不足时临时存放数据，是虚拟内存的一部分。**

---

## 3. `/home` 为什么常被单独分区

答：

**因为它存放用户数据和个人配置，单独分出来有利于系统维护和重装时保留数据。**

---

## 4. `/boot` 是做什么的

答：

**用于存放系统启动相关文件。**

---

## 5. Ubuntu 常见文件系统是什么

答：

**ext4**

---

## 6. Windows 常见文件系统是什么

答：

**NTFS**

---

# 第15页：安装后还要做什么

这一页写的是：

1. install: Partitioning (Boot area setup)
2. network: `sudo pppoeconf` / Campus Network mentohust / `ifconfig`
3. Install the software center or synaptic
   `sudo apt-get install synaptic`

---

## 这一页在讲什么

这一页其实是在说：

**Ubuntu 不是装完就结束了，装完后还要继续做几个基本配置。**

主要有三件事：

第一，检查分区和引导区设置；
第二，配置网络；
第三，安装软件管理工具。

也就是说，前一页讲的是“怎么分区”，这一页讲的是“系统装好之后，怎么让它真正能正常用”。

---

## 1. Partitioning (Boot area setup)

这部分和上一页分区内容是连着的。

### 它的意思

这里的 **Partitioning** 就是分区，
**Boot area setup** 就是启动区域设置。

安装 Ubuntu 时，不光要分出根分区 `/`、交换分区 `swap`，还要注意：

* 启动文件放在哪里
* 引导程序装在哪里
* 双系统时会不会影响原来的 Windows 启动

### 为什么老师要单独提这个

因为很多同学装系统失败，不是系统本身有问题，而是：

* 分区没分好
* 启动区没设对
* 引导位置选错了

### 你要记住的点

这一步的核心不是“背单词”，而是明白：

**Linux 安装不只是复制文件，还涉及磁盘分区和启动引导配置。**

---

## 2. network：`sudo pppoeconf` / mentohust / `ifconfig`

这部分是在讲 **装完系统后先让网络通起来**。

### （1）`sudo pppoeconf`

这是一个和 **PPPoE 拨号上网** 有关的命令。

#### PPPoE 是什么

PPPoE 可以理解成一种宽带拨号认证方式。
有些校园网、宿舍网或者老一点的网络环境，需要输入账号密码进行拨号认证。

#### 这条命令想表达什么

老师这里的意思是：

**如果网络环境需要拨号认证，可以用相关工具进行配置。**

#### 你课堂上怎么记

`sudo pppoeconf`
= 用管理员权限配置 PPPoE 网络连接

---

### （2）Campus Network mentohust

这行是在说：

**校园网环境下，可能会用到 mentohust 这样的认证工具。**

有些学校网络不是普通家用宽带那种直接联网，而是要走专门的校园网认证客户端，所以老师把它列出来，是想告诉你：

**Linux 下也需要解决认证上网问题。**

你可以把它理解成：

> 这是“校园网登录/认证”的一种工具思路。

---

### （3）`ifconfig`

这个命令是用来查看和配置网络接口信息的。

#### 能看到什么

它通常能显示：

* 网卡信息
* IP 地址
* 子网掩码
* 网络接口状态

#### 这一页提它的目的

就是让你知道：

**配网络时，不只是连上就行，还要会查看网络状态。**

比如你装完 Ubuntu 后想检查：

* 网卡识别了没有
* 有没有 IP
* 网络接口是不是 up
* 当前机器地址是多少

这时候就会用到这类网络查看命令。

---

## 3. 安装 software center 或 synaptic

这一部分是在讲：

**装完系统之后，要装一个更方便的软件管理工具。**

课件给出的命令是：

```bash
sudo apt-get install synaptic
```

### Synaptic 是什么

Synaptic 是一个图形化的软件包管理工具。

你可以理解成：

**它相当于一个更直观的软件安装管理界面。**

也就是说，你不用每次都纯手打命令，也可以通过图形界面：

* 搜索软件
* 安装软件
* 删除软件
* 查看依赖关系

### 为什么这里要讲它

因为 Ubuntu 虽然可以完全靠命令行装软件，但对初学者来说，图形化工具更容易上手。

### 这条命令怎么理解

* `sudo`：以管理员权限执行
* `apt-get`：Ubuntu 常见的软件包管理命令
* `install`：安装
* `synaptic`：要安装的软件名

整句就是：

**用管理员权限，通过 apt-get 安装 synaptic。**

---

# 第16页：Su authentication failed

这一页内容很短，就两行：

* Su authentication failed
* `sudo passwd`

但它很关键，因为这是 Linux 初学者很容易遇到的问题。

---

## 这一页在讲什么

它在讲：

**当你想切换到 root 用户或者使用 su 时，认证失败怎么办。**

---

## 1. 什么叫 `su authentication failed`

### `su` 是什么

`su` 的意思可以理解成：

**切换用户身份**，最常见就是切换到 root。

比如输入：

```bash
su
```

系统会要求你输入 root 密码。

### authentication failed 是什么

意思就是：

**认证失败，密码没通过。**

也就是说：

* 你输入的 root 密码不对
* 或者 root 账户没有按你预期设置好
* 所以没法通过 `su` 切过去

---

## 2. 为什么会出现这个问题

因为在很多 Ubuntu 使用场景里，平时更多使用的是 `sudo`，而不是直接长期切到 root。

也就是说，系统更鼓励你：

**需要管理员权限时，临时在命令前加 `sudo`。**

所以很多同学第一次装好 Ubuntu 后，会发现：

* 自己用户可以 `sudo`
* 但直接 `su` 不一定顺手就能用

---

## 3. `sudo passwd` 是什么意思

这页给出的解决命令是：

```bash
sudo passwd
```

### 这条命令在干什么

`passwd` 是设置密码的命令，
前面加 `sudo` 表示以管理员权限执行。

老师想表达的是：

**当认证有问题时，可以通过管理员权限去重新设置密码。**

### 这页你要抓住的本质

不是死背这一条命令，而是理解：

**Linux 里 root 权限管理和普通用户权限管理是分开的，认证失败时往往要从密码和权限配置入手。**

---

## 4. `su` 和 `sudo` 的区别

这个特别容易考。

### `su`

是 **切换用户**，通常切换到 root。

### `sudo`

是 **以管理员权限执行某一条命令**。

所以：

* `su` 更像是“变成 root”
* `sudo` 更像是“借用一次 root 权限”

### 最简单记忆

* `su`：切换身份
* `sudo`：借权限执行命令

---

# 第17页：Several popular Linux distributions

这一页主要是图片，标题是：

**Several popular Linux distributions**
也就是：

**几种流行的 Linux 发行版**

图里能看到的大概有：

* Red Hat
* Fedora
* Debian
* Ubuntu
* SUSE
* Gentoo
* Mandriva
* Slackware（左上角那个 S）

---

## 这一页在讲什么

这页最核心的意思是：

**Linux 不是只有一种。**

很多同学会误以为 Linux 就是一个统一版本，其实不是。
Linux 更准确地说，有很多不同的 **发行版（distribution，简称 distro）**。

---

## 1. 什么叫 Linux 发行版

你可以这样理解：

* Linux 内核只是核心
* 真正给用户用的系统，还需要加上很多工具、软件包、安装器、桌面环境、包管理器等
* 把这些组合好，形成一套可安装、可使用的系统，就是一个 Linux 发行版

所以：

**发行版 = Linux 内核 + 系统工具 + 软件管理方式 + 默认配置 + 生态风格**

---

## 2. 为什么会有这么多发行版

因为不同发行版的目标不同。

有的偏：

* 稳定
* 服务器
* 企业级

有的偏：

* 新功能
* 桌面易用性
* 学习和实验

还有的偏：

* 高度可定制
* 源码构建
* 高级用户

所以不是谁“更高级”，而是面向人群不同。

---

## 3. 这页几个典型发行版怎么理解

### Red Hat

偏企业级、商业化影响力大。
很多企业 Linux 生态会受到它影响。

### Fedora

通常可以理解成一个比较新、比较前沿的方向，和 Red Hat 家族关系很近。

### Debian

非常经典、稳定、影响力大。
很多发行版和它关系很深。

### Ubuntu

基于 Debian 体系，桌面用户、初学者、教学环境里非常常见。
你们课程装的就是这个。

### SUSE

也是很有代表性的一个发行版体系。

### Gentoo

强调高度定制化，很多东西可以按自己的需求来构建。
更偏向对 Linux 理解比较深入的用户。

### Slackware

历史比较久，风格相对传统。

### Mandriva

也是早期较有代表性的发行版之一。

---

## 4. 这一页老师真正想让你记什么

不是让你把每个 logo 全背下来，
而是让你知道：

**Linux 生态是多发行版并存的。**

而且不同发行版会带来不同的：

* 包管理方式
* 软件格式
* 使用习惯
* 配置方式

这就自然引出下一页：**包管理**。

---

# 第18页：Package management

这一页写的是：

1. Red Hat FC Centos yum rpm (.rpm)
2. ubuntu apt dpkg (.deb)
3. suse zypper
4. gentoo emerge

---

## 这一页在讲什么

这页讲的是：

**不同 Linux 发行版，管理软件的方式不一样。**

也就是说，在 Windows 里你可能习惯双击 `.exe` 安装，
但在 Linux 里，更常见的是通过 **包管理系统** 来安装和管理软件。

---

## 1. 什么叫 Package management

Package management 就是：

**软件包管理**

它包括：

* 安装软件
* 卸载软件
* 更新软件
* 解决依赖关系
* 查询软件信息

所以包管理系统不是单纯“安装器”，而是一整套软件维护机制。

---

## 2. 为什么 Linux 特别强调包管理

因为 Linux 系统里的软件很多不是单独存在的，而是互相依赖的。

比如你装一个软件，可能还需要：

* 某个库
* 某个运行环境
* 某个依赖包

如果全靠手动处理会非常麻烦，
所以 Linux 发展出了专门的包管理系统来统一维护。

---

## 3. 这一页列的内容怎么理解

---

### （1）Red Hat / Fedora / CentOS：`yum`、`rpm`、`.rpm`

这里表示的是一整个体系。

#### `rpm`

是一种软件包格式，也是一套包管理基础工具。
`.rpm` 是这种软件包的文件后缀。

#### `yum`

可以理解成更高层的软件管理命令，能更方便地处理依赖和安装。

所以这一组的逻辑是：

* 包格式：`.rpm`
* 基础体系：rpm
* 常用管理工具：yum

---

### （2）Ubuntu：`apt`、`dpkg`、`.deb`

这组是你最应该重点记的，因为你们课里用 Ubuntu。

#### `.deb`

是 Debian/Ubuntu 体系常见的软件包格式。

#### `dpkg`

是较底层的软件包管理工具。

#### `apt`

是更常见、更方便的高级管理工具，用来：

* 安装
* 更新
* 删除
* 搜索软件包

所以这组关系是：

* 包格式：`.deb`
* 底层工具：dpkg
* 高层工具：apt

### 最容易考的一句

**Ubuntu 常用 apt 管理软件包，软件包格式是 .deb。**

---

### （3）SUSE：`zypper`

SUSE 体系常见的软件管理工具是 `zypper`。

你可以记成：

**SUSE → zypper**

---

### （4）Gentoo：`emerge`

Gentoo 常见的软件管理工具是 `emerge`。

你可以记成：

**Gentoo → emerge**

---

## 4. 这一页最重要的记忆方法

你不要把它看成一堆散乱命令，
而是记成：

**不同发行版 = 不同软件生态 = 不同包管理命令**

最常考、最有用的是这两组：

* **Red Hat 系：rpm / yum**
* **Ubuntu 系：deb / dpkg / apt**

---

## 5. `apt` 和 `dpkg` 的区别

这个也很容易混。

### `dpkg`

更底层，直接处理 `.deb` 包。

### `apt`

更高层，更适合日常使用，能自动处理依赖。

所以平时更常说：

* `apt install`
* `apt update`

而不是总直接用 `dpkg`。

---

# 这4页串起来的总逻辑

你可以把第15～18页连成一条线：

**Ubuntu 装完以后，要继续做分区/引导检查、网络配置和软件工具安装；使用过程中如果遇到 su 认证失败，要理解 sudo 和密码设置；Linux 有很多不同发行版，而不同发行版最大的差别之一，就是软件包管理方式不同。**

---

# 第15～18页总结

1. Ubuntu 安装完成后，还要继续处理启动区、网络和软件管理工具。
2. 网络配置可能涉及 PPPoE、校园网认证，以及用 `ifconfig` 查看网络状态。
3. `su authentication failed` 说明切换用户时认证失败；`su` 是切换身份，`sudo` 是临时使用管理员权限。
4. Linux 有很多发行版，常见有 Red Hat、Fedora、Debian、Ubuntu、SUSE、Gentoo 等。
5. 不同发行版的重要区别之一，是软件包格式和包管理方式不同。
6. Ubuntu 常用 `apt` 和 `dpkg` 管理 `.deb` 包，Red Hat 系常见 `yum` 和 `rpm` 管理 `.rpm` 包。

---

## 速记
**Ubuntu 装完后要继续配置启动、网络和软件管理；`su` 认证失败说明权限或密码有问题；Linux 有很多发行版；不同发行版的重要差异之一是软件包管理方式不同，Ubuntu 主要用 `apt/dpkg/.deb`。**
