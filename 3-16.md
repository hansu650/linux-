1. tty123456
- 两个图形界面，四个命令行
- 早期图形界面不稳定-也是一个软件
- 所以切换别的命令行界面区修复图形界面

2. 每个分区做不用的工作

3. 默认是普通用户,安装系统软件不允许
- sudo 
- su - 就可以一步一步敲了

4. apt 在线安装
- 链接的是software repository
- chmod 666 可读可写不可执行
- 666-110             777-111

- apt update 检查软件最新版本

5. bin sbin
- bin-binary 可执行文件 命令
- 大部分能在terminal敲的都在这里

6. boot 驱动
- dev 设备 


7. 改引导程序
- chmod 666 grub.cfg
- chmod 666 /boot/grub/grub.cfg
相对和绝对路径改这个文件

- 第 9 步是在学：GRUB 的启动菜单不是随手写死的，而是由 /etc/default/grub 和 /etc/grub.d/* 生成到 /boot/grub/grub.cfg，改完要用 update-grub 重新生成

8. boot 操作系统
- config, vmlinuz, system.map,initrd.img
- config 配置，做功能选项
- 所以到时候直接拷贝回去就行了，因为和当前系统兼容
- config的 * 核心功能
- 内存，终端，写在vmlinuz
- 差模块放在initrd.img 设备驱动，把文件系统挂上来
- config的最后两行执行vmlinuz, initrd.img
-  system.map是vmlinuz的映射文件(变量名和地址的对)

9. 引导顺序   /efi/boot/grubx64.efi(/dev/sda2 mount to /boot/efi)   -   grub.cfg    -   vmlinuz    -    initrd.img